# OS
.DS_Store

# Build artifacts — transplant.mjs output and Jekyll
_entries/
_site/
.jekyll-cache/

# Node (js-yaml is installed ad hoc, never committed)
node_modules/
package-lock.json
package.json

# Obsidian per-user workspace state
vault/.obsidian/workspace*
